[project]
name = "aletheia"
version = "0.1.0"
description = "AIGC text detector API service"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.133.1",
    "langdetect>=1.0.9",
    "torch>=2.10.0",
    "transformers>=5.2.0",
    "uvicorn[standard]>=0.41.0",
]
